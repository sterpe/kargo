# payload

```
var Payload = require('payload').Payload
, payload = new Payload('FOO', 'a', 'b', 1, 2)
;

/*
 * {
 * 	type: "FOO"
 *	, actionType: "FOO"
 *	, arguments: ['a', 'b', 1, 2]
 * }
 *
 */
```
